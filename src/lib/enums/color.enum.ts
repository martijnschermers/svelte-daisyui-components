export enum Color {
    Primary = 'primary',
    Secondary = 'secondary',
    Accent = 'accent',
    Neutral = 'neutral',
    Success = 'success',
    Error = 'error',
    Warning = 'warning',
    Info = 'info',
    Ghost = 'ghost'
};

export type ColorStrings = 'primary' | 'secondary' | 'accent' | 'neutral' | 'success' | 'error' | 'warning' | 'info' | 'ghost' | '';
